name: Build ARM32 Linux

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install System Deps
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake pkg-config libwayland-dev libx11-dev libxkbcommon-dev libgles2-mesa-dev zip curl

      - name: Manual Flutter Download
        run: |
          curl -L https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.41.2-stable.tar.xz -o flutter.tar.xz
          tar xf flutter.tar.xz
          mv flutter $HOME/custom_flutter
          echo "$HOME/custom_flutter/bin" >> $GITHUB_PATH
          echo "$HOME/custom_flutter/bin/cache/dart-sdk/bin" >> $GITHUB_PATH

      - name: Setup Sony Tool
        run: |
          git clone https://github.com/sony/flutter-elinux.git
          echo "$GITHUB_WORKSPACE/flutter-elinux/bin" >> $GITHUB_PATH

      - name: Build for ARM32
        env:
          # This tells the Sony tool EXACTLY where our good Flutter is
          ELINUX_FLUTTER_SDK: ${{ github.workspace }}/../custom_flutter
        run: |
          cd my_app
          # 1. First, run standard pub get with the good Dart
          flutter pub get
          
          # 2. Run create but tell it where the SDK is
          flutter-elinux create --flutter-sdk=$HOME/custom_flutter .
          
          # 3. Build using the specific SDK
          flutter-elinux build elinux --target-arch=arm --target-backend-type=x11 --release --flutter-sdk=$HOME/custom_flutter

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-arm32-app
          path: my_app/build/elinux/arm/release/bundle/
